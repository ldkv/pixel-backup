# Copy to .env and customize. All values are optional; defaults shown below.

# Path to the users config file (ignored inside Docker — container always uses /app/configs/users.json).
USER_CONFIGS=/app/configs/users.json

# Host directory bind-mounted into the container at the same path.
# MUST be a common parent of every user's source_dir AND of SYNCTHING_DIR,
# so that hard links between them stay on the same filesystem.
# Example layout:
#   /mnt/media/library/alice/   <- user source_dir
#   /mnt/media/library/bob/     <- user source_dir
#   /mnt/media/syncthing/       <- SYNCTHING_DIR (hard-link staging)
DATA_ROOT=/mnt/media

# Directory where hard links are created for Syncthing. Must live under DATA_ROOT.
SYNCTHING_DIR=/mnt/media/syncthing

UPPER_LIMIT_GB=20.0
CRON_SCHEDULE=0 0 * * *
TIMEZONE=UTC
MIN_SLEEP_SECONDS=60

# Optional Discord webhook for sync notifications. Leave unset to disable.
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxx/yyy
